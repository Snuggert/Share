#ifndef WALKER_H
#define WALKER_H

/* Incomplete type declaration. */
typedef struct {
    ...
} walker_t;


/* Incomplete declarations of the support functions. */
walker_t* init_walker(...);
int move_walker( ... );
int at_exit( ... );
int check_move( ... );
void cleanup_walker( ... );

#endif
